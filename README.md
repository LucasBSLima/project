# Vite React TypeScript Starter

This project uses [Vite](https://vitejs.dev/) and React. Tests are run with [Vitest](https://vitest.dev/).

## Running tests

Install dependencies and run:

```bash
npm test
```

Vitest will execute the test suite once.
